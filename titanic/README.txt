This folder is a binary classification for the survivors of the titanic
The dataset had non-uniform data so I had to clean it up for ml use
used the same model as the previous one

When you run this python file, it reads the CSV inside the folder, run it through the model, then outputs loss, and accuracy of the model
